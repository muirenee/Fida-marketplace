'use client';
import {useEffect,useState} from 'react';
export function PromotionPolicy(){
 const [data,setData]=useState<any>(null),[error,setError]=useState(''),[busy,setBusy]=useState(false);
 useEffect(()=>{fetch('/api/admin/promotion-policy').then(async r=>{const d=await r.json();if(!r.ok)throw Error(d.message||d.error);setData(d);}).catch(e=>setError(String(e)));},[]);
 return <section className="mp-panel"><h2>Platform promotion rules</h2>{error&&<p className="mp-error" role="alert">{error}</p>}{data&&<form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/admin/promotion-policy',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({maxPercent:Number(data.maxPercent),maxDiscount:Number(data.maxDiscount),allowStacking:data.allowStacking})});const d=await r.json();if(!r.ok)throw Error(d.message||d.error);setData(d);}catch(e){setError(String(e));}finally{setBusy(false);}}}><label>Maximum new promotion percentage<input type="number" min="1" max="100" required value={data.maxPercent} onChange={e=>setData({...data,maxPercent:e.target.value})}/></label><label>Maximum discount cap (merchant currency)<input type="number" step="0.01" min="0.01" max="100000000" required value={data.maxDiscount} onChange={e=>setData({...data,maxDiscount:e.target.value})}/></label><label><input type="checkbox" checked={data.allowStacking} onChange={e=>setData({...data,allowStacking:e.target.checked})}/> Allow item promotions and cart coupons together</label><p>Percentage/cap limits apply to newly created promotions. Disabling stacking takes effect at the next checkout.</p><button disabled={busy}>Save promotion rules</button></form>}</section>;
}
